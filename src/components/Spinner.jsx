import React from 'react';
import './Spinner.css'; // We'll style the spinner here

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;